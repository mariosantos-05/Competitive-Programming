//comentario é feito com as duas barras //

//bibliotecas podem seer adcionadas do seguinte modo com "#"

#include <iostream>

int main() {
    std::cout << "Hellow, world. program to work... \n";
    return 0;
}

// para rodar os codigos é necessario compilar antes de todo novo teste, para isso é só usar "g++ codigo.formato" é possivel simplismente ficar recompilando o mesmo 'a.out' varias vezes. se quiser salvar uma parte diferente é só nomear.

